// Longhand:
let likeJavaScript;

if (likeJavaScript === true) {
  // something here
}

// Shorthand:

if (likeJavaScript) {
  // something here
}

// Longhand:

let variableName;
if (variableName !== true) {
  // do something...
}

// Shorthand:

let variableName;
if (!variableName) {
  // do something...
}
